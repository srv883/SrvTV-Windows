<Window x:Class="SrvTv.ScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Schedule" Height="800" Width="760" Background="#0E1118"
        PreviewKeyDown="OnPreviewKeyDown">
    <Grid Margin="36,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ChanTitle" Text="" Foreground="White" FontSize="26" FontWeight="Bold"/>
        <TextBlock x:Name="DateLine" Text="" Foreground="#9AA2B0" FontSize="14" Margin="0,4,0,18"/>
        <ListBox x:Name="Rows" Grid.Row="2" Background="Transparent" BorderThickness="0"
                 ScrollViewer.CanContentScroll="True">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="16,12"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="Transparent" CornerRadius="10"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#23262F"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Time}" Foreground="#5AC8FA" FontSize="15"
                                       FontWeight="Bold" Width="150"/>
                            <TextBlock Text="{Binding Title}" Foreground="{Binding TitleColor}"
                                       FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Desc}" Foreground="#9AA2B0" FontSize="13"
                                   TextWrapping="Wrap" Margin="150,4,0,0"
                                   Visibility="{Binding HasDesc, Converter={StaticResource BoolVis}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Resources>
                <BooleanToVisibilityConverter x:Key="BoolVis"/>
            </ListBox.Resources>
        </ListBox>
        <TextBlock x:Name="Empty" Grid.Row="2" Text="No schedule for this channel."
                   Foreground="#9AA2B0" FontSize="16" Visibility="Collapsed"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
